version: "3.1"

services:
        # контейнер с сервером
    server:

        build: .

        ports:  
        - "8080:8080"

        command: ./MyFirstService
    
        # контейнер с базой
    mysqldb:

        image: mysql:5.7.32

        environment: 
            - MYSQL_ROOT_PASSWORD=1643
         
    
    
        
            
      

